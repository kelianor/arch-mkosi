#!/bin/bash
set -e

mkdir -p "/usr/lib/confexts/Factory/etc"
cd "/usr/lib/confexts/Factory/etc"
cp -r "/etc" .

mkdir -p "./etc/extension-release.d/"
touch "./etc/extension-release.d/extension-release.Factory"

echo "ID=_any" >> "./etc/extension-release.d/extension-release.Factory"
